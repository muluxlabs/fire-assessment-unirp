<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform Material theme so no AppCompat dependency is required. -->
    <style name="Theme.UniRP" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_green_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorPrimary">@color/brand_green</item>
    </style>
</resources>
