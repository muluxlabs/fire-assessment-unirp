// Top-level build file. Versions match the toolchain already present on this
// machine (AGP 9.0.1 / Kotlin 2.3.20 / Gradle 9.1.0) so no large downloads.
plugins {
    // AGP 9.0 provides built-in Kotlin support, so no separate Kotlin plugin.
    id 'com.android.application' version '9.0.1' apply false
}
